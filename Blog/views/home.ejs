<%- include('./partials/header'); -%>
<!-- replace with header -->

<h1>Home</h1>

<p> <%= homeStartingContent %></p>

<!-- using a new kind of for loop in array -->
<% Posts.forEach(function(post){ %>
  <div>
    <h1><%= post['title']%></h1>
    <% console.log("/posts/"+post.title) %>
    <p>
      <%= post['post'].substring(0,100) + " ... "%>
      <a href = "/posts/<%=post.title%>" >Read More</a>
    </p>
  </div>
<% }) %>



<!-- printing out all the posts that are pushed in the Posts array -->


<%- include('./partials/footer'); -%>
<!-- replace with footer -->

